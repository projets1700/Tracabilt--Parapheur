version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: tracaparapheur-db
    environment:
      POSTGRES_DB: tracaparapheur
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: tracaparapheur2025
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
